<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Dianty.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:Dianty.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DataContext="{d:DesignInstance Type=vm:MainViewModel}"
    mc:Ignorable="d">

    <Grid RowDefinitions="Auto,*" ColumnDefinitions="8,*,Auto,*,8">
        <!--<ComboBox Grid.Row="0" Grid.Column="2" Width="100" VerticalAlignment="Center"
                ItemsSource="{x:Bind ViewModel.PidList, Mode=OneWay}"
                SelectedIndex="{x:Bind ViewModel.SelectedPidIndex, Mode=TwoWay}"
                SelectedItem="{x:Bind ViewModel.SelectedPidItem, Mode=TwoWay}"/>-->
        <TextBlock Grid.Row="0" Grid.Column="2" Text="{Binding CurrentPid, Mode=OneWay}" Margin="0,8,0,0" VerticalAlignment="Center"/>
        <ScrollViewer Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="5" Padding="8">
            <Grid RowDefinitions="5,Auto,5,Auto" ColumnDefinitions="Auto,*">
                <TextBlock Grid.Row="1" Grid.Column="0" Text="当前血量：" VerticalAlignment="Center" HorizontalAlignment="Right"/>
                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding CurrentHealth, Mode=OneWay}" VerticalAlignment="Center"/>
                <TextBlock Grid.Row="3" Grid.Column="0" Text="当前护甲：" VerticalAlignment="Center" HorizontalAlignment="Right"/>
                <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding CurrentArmor, Mode=OneWay}" VerticalAlignment="Center"/>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
